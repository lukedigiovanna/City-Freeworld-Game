package main;

import misc.MathUtils;
import misc.Vector2;

public class Main {
	public static void main(String[] args) {
		Program.init();
		
//		Vector2 rp0 = new Vector2(0,0), rp1 = new Vector2(0,1);
//		Vector2 ep = new Vector2(1,2);
//		
//		float theta = (float)MathUtils.getAngle(rp0, rp1);
//		float beta = (float)MathUtils.getAngle(rp1, ep);
//		float gamma = theta - beta;
//		float sigma = (float)Math.PI/2-gamma;
//		float d = MathUtils.distance(rp1, ep);
//		float A = (float)Math.cos(sigma) * d;
//		float alpha = -(float)(Math.PI/2 - theta);
//		Vector2 reference = new Vector2(rp1.x + (float)Math.cos(alpha)*A, rp1.y + (float)Math.sin(alpha) * A);
//		System.out.println(reference);
	}
}
